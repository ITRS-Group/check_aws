[pytest]
testpaths = tests
addopts = --cov nagios_aws
log_level = DEBUG
log_format = %(asctime)s %(name)s [%(filename)s:%(lineno)d] %(levelname)s: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
filterwarnings =
    ignore::DeprecationWarning
